@model Walnut.Entities.CandidateFile


<p><b>Are you sure you want to delete this?</b></p>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.FileName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FileName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Size)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Size)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TimeStamp)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimeStamp)
        </dd>

    </dl>
    @*@Html.AntiForgeryToken()*@
</div>
        <div class="form-actions no-color">
            @Ajax.ActionLink(linkText: "Delete",
                    actionName: "DeleteFile",
                    routeValues: new { id = Model.Id },
                    ajaxOptions: new AjaxOptions
                    {
                        HttpMethod = "Post",
                        UpdateTargetId = "ajaxtarget",
                        InsertionMode = InsertionMode.Replace,
                        OnComplete = "countCandidateFiles"
                    })
            &nbsp;&nbsp;
            <a href="#" id="CancelFileDeletion">Cancel File Deletion</a>
        </div>

<script>
    //div self destruction script (this partial view lives in "ajaxtargetSecondary" div which is about to get removed )
    $("#CancelFileDeletion").click(function () {

        $(".FileDeletionDialogArea").html("");
        return false;
    });
</script>
